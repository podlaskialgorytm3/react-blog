import { PostContent } from '../types/post-content';

export const DEFAULT_POST: PostContent = {
    postId: 0,
    userId: 0,
    title: '',
    content: ''
}
export const DEFAULT_POST_ERRORS: PostContent = DEFAULT_POST;